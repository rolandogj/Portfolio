@model Portfolio.Models.SocialLink

@{
    ViewBag.Title = "Social Link Details";
}

<h2>@Model.Platform</h2>

<dl class="dl-horizontal">
    <dt>URL</dt>
    <dd><a href="@Model.Url" target="_blank">@Model.Url</a></dd>

    <dt>Student</dt>
    <dd>@Model.StudentProfile.FullName</dd>
</dl>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>

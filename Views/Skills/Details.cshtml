@model Portfolio.Models.Skills

@{
    ViewBag.Title = "Skill Details";
}

<h2>@Model.Name</h2>

<dl class="dl-horizontal">
    <dt>Level</dt>
    <dd>@Model.Level</dd>

    <dt>Student</dt>
    <dd>@Model.StudentProfile.FullName</dd>
</dl>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
